h2 Create Blog Post {{Usernames}} 
.hero
    form(name="blogPostForm" 
        ng-controller="adminBlogController"
        ng-submit="createPost()"
        )
            input.form-control(name="input" 
                placeholder="Post Title"
                ng-model="formData.title" required)
            text-angular(ng-model="formData.body")
            input(type="file" nv-file-select uploader="{FileUploader}" options="{Object}")
            input.form-control.btn(type="submit" id="submitnew" value="Create")
            {{formData}}

input(type="text" ng-model="myModelObj")
input(type="file" ng-file-select="onFileSelect($files)")
input(type="file" ng-file-select="onFileSelect($files)" multiple accept="image/*")
div.button(ng-file-select="onFileSelect($files)" data-multiple="true")
div(ng-file-drop="onFileSelect($files)" ng-file-drag-over-class="optional-css-class-name-or-function" ng-show="dropSupported") 
    drop files here
div(ng-file-drop-available="dropSupported=true" ng-show="!dropSupported")
    HTML5 Drop File is not supported!
button(ng-click="upload.abort()") Cancel Upload





